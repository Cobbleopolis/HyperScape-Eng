package com.cobble.hyperscape.reference

object CameraRef {

    object CameraMode extends Enumeration {
        type CameraMode = Value
        val PERSPECTIVE_MODE, ORTHOGRAPHIC_MODE = Value
    }
    
}
